<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Checklist Dashboard</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="dashboard">
    <h1>Checklist Dashboard</h1>
    <ul class="checklist">
      <% results.forEach(result => { %>
        <li class="<%= result.passed ? 'passed' : 'failed' %>">
          <%= result.rule %>: <%= result.passed ? 'Passed' : 'Failed' %>
        </li>
      <% }) %>
    </ul>
  </div>
</body>
</html>
